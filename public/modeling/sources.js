export async function json(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw Error(`${url}: HTTP ${r.status}`);return r.json();}
export async function loadSources(){const [examples,players,personnel,availability,availabilityConfig,influence,empirical,presets,manifest]=await Promise.all(['/drive-lab/examples.json','/drive-lab/players.json','/drive-lab/personnel.json','/drive-lab/availability.json','/drive-lab/availability-config.json','/drive-lab/personnel-config.json','/drive-lab/live-empirical.json','/modeling/presets.json','/research/latest.json'].map(json));const v1=await json(manifest.snapshot.simulation_path);const health=await json('/drive-lab/availability-status.json').catch(()=>({status:'unknown'}));availability.source_status=health.status;availability.source_attempt=health.attempted_at;return {examples,players,personnel,availability,availabilityConfig,influence,empirical,presets,v1,manifest};}
export const escape=v=>String(v??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const number=(v,n=1)=>typeof v==='number'&&Number.isFinite(v)?v.toFixed(n):'—';
