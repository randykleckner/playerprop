const endpoint = "/api/signals/live?historySeason=2025&limit=100";
const page = document.querySelector("#story-page"), error = document.querySelector("#error"), theme = document.querySelector("#theme-toggle");
const escapeHtml = (value) => String(value).replace(/[&<'"]/g, (character) => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", "'":"&#39;", '"':"&quot;" })[character]);
const number = (value) => Number(value).toLocaleString(undefined, { maximumFractionDigits: 1 });
const id = new URLSearchParams(location.search).get("id");
function setTheme(value) { const dark = value === "dark"; document.body.dataset.theme = dark ? "dark" : "light"; theme.textContent = dark ? "Light mode" : "Dark mode"; localStorage.setItem("drlocks-theme", dark ? "dark" : "light"); }
function render(signal) { const gap = Math.abs(signal.recentAverage - signal.line), direction = signal.recentAverage >= signal.line ? "above" : "below", side = signal.direction === "over" ? "OVER" : "UNDER"; const book = signal.sportsbook ? String(signal.sportsbook).replace(/[_-]/g, " ") : "market average"; page.innerHTML = `<article class="case-file"><p class="eyebrow">CASE FILE · ${escapeHtml(signal.matchup)}</p><h1>${escapeHtml(signal.playerName)} — ${side} ${escapeHtml(number(signal.line))} ${escapeHtml(signal.marketLabel)}</h1><p class="case-lede"><strong>The betting line:</strong> ${escapeHtml(signal.playerName)} over or under ${escapeHtml(number(signal.line))} ${escapeHtml(signal.marketLabel)}. This case looks at the <strong>${side}</strong>. The line is from ${escapeHtml(book)}.</p><section class="case-hero"><div><span>LAST ${escapeHtml(signal.recentGames)} GAMES</span><strong>${escapeHtml(number(signal.recentAverage))}</strong><small>${escapeHtml(signal.marketLabel)} per game</small></div><div><span>COMPARED WITH THE LINE</span><strong class="${direction}">${escapeHtml(number(gap))}</strong><small>${escapeHtml(direction)} ${escapeHtml(number(signal.line))}</small></div><div><span>OPPONENT</span><strong>#${escapeHtml(signal.defenseRankFewestAllowed)}</strong><small>league rank vs ${escapeHtml(signal.position)}s</small></div></section><section class="case-evidence"><div><p class="eyebrow">THE NUMBERS</p><h2>What needs to happen</h2><p>For the Over, ${escapeHtml(signal.playerName)} needs more than ${escapeHtml(number(signal.line))} ${escapeHtml(signal.marketLabel)}. For the Under, he needs fewer. He has averaged ${escapeHtml(number(signal.recentAverage))} across his last ${escapeHtml(signal.recentGames)} games.</p></div><div><p class="eyebrow">THE MATCHUP</p><h2>What the defense has allowed</h2><p>${escapeHtml(signal.opponentTeam)} ranks #${escapeHtml(signal.defenseRankFewestAllowed)} in the league for ${escapeHtml(signal.marketLabel)} allowed to ${escapeHtml(signal.position)}s. That ranking is team context, not a guarantee about one player.</p></div><div><p class="eyebrow">THE ROLE</p><h2>How much he has played</h2><p>${signal.recentSnapShare == null ? "Playing-time information is not available for these games." : `${escapeHtml(signal.playerName)} played ${escapeHtml(number(signal.recentSnapShare))}% of his team’s offensive snaps in the last ${escapeHtml(signal.recentGames)} games.`} Availability and teammate context stay part of the final call.</p></div></section></article>`; }
async function load() { try { const response = await fetch(endpoint, { cache:"no-store" }); if (!response.ok) throw new Error(); const data = await response.json(); const signal = (data.signals || []).find((item) => `${item.playerId}|${item.marketKey}|${item.eventId}` === id); if (!signal) throw new Error(); render(signal); } catch { error.hidden = false; error.textContent = "This case file is not available right now. Return to the Sunday Edition and try again."; } }
setTheme(localStorage.getItem("drlocks-theme") || "dark"); theme.addEventListener("click", () => setTheme(document.body.dataset.theme === "dark" ? "light" : "dark")); load();
