import {loadResearch,readinessHtml} from './readiness.js';
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
try{const manifest=await loadResearch();$('readiness').innerHTML=readinessHtml(manifest);const response=await fetch(manifest.snapshot.identities_path);if(!response.ok)throw Error('Identity report unavailable');const report=await response.json();
const render=()=>{const rows=report.rows.filter(r=>!$('filter').value||r.confidence===$('filter').value);$('counts').textContent=`${rows.length} of ${report.rows.length} offensive salary entries · draft group ${report.slate_id}`;$('rows').innerHTML=rows.map(r=>`<tr><td>${esc(r.player_name)}</td><td>${esc(r.team)} ${esc(r.position)}</td><td>${esc(r.confidence)}</td><td>${esc(JSON.stringify(r.external_ids))}</td><td>${esc(r.canonical_player_id||r.candidates.join(', ')||'No candidate')}</td></tr>`).join('');};$('filter').onchange=render;render();$('exclusions').innerHTML=Object.entries(report.exclusions).map(([reason,count])=>`<li>${esc(reason.replaceAll('_',' '))}: ${count}</li>`).join('');
}catch(e){$('readiness').textContent=e.message;}
