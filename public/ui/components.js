export const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const money = value => Number.isFinite(value) ? new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(value) : '—';
export const decimal = value => Number.isFinite(value) ? value.toFixed(1) : '—';
export const option = (value,label) => `<option value="${esc(value)}">${esc(label)}</option>`;
export const avatar = p => `<span class="ui-avatar" data-ui-player="${esc(p.player_id||'')}" aria-hidden="true">${esc(p.position==='DST'?p.team:p.player_name.split(' ').map(s=>s[0]).slice(0,2).join(''))}</span>`;
export const teamMark = team => `<span class="ui-team-mark" data-ui-team="${esc(team)}" aria-hidden="true">${esc(team)}</span>`;
export function matchupGraphic(game){return `<div class="ui-matchup-hero"><div class="ui-matchup-team">${teamMark(game.awayTeam)}<strong>${esc(game.awayTeam)}</strong><small>Away</small></div><div class="ui-matchup-center"><span class="ui-matchup-vs">VS</span><span>${esc(new Date(game.market.start_time).toLocaleString(undefined,{weekday:'short',hour:'numeric',minute:'2-digit'}))}</span><div class="ui-field-mark" aria-hidden="true"><i></i><i></i><i></i><i></i><i></i></div><small>Game total <b>${decimal(game.market.total)}</b></small></div><div class="ui-matchup-team">${teamMark(game.homeTeam)}<strong>${esc(game.homeTeam)}</strong><small>Home</small></div></div>`;}
export function relativeTime(value,now=Date.now()){const age=now-Date.parse(value);if(!Number.isFinite(age))return 'Time unavailable';if(age<0)return 'Just updated';const minutes=Math.floor(age/60000);return minutes<1?'Just updated':minutes<60?`${minutes}m ago`:minutes<1440?`${Math.floor(minutes/60)}h ago`:`${Math.floor(minutes/1440)}d ago`;}
