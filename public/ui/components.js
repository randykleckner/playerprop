export const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export const money = value => Number.isFinite(value) ? new Intl.NumberFormat('en-US',{style:'currency',currency:'USD',maximumFractionDigits:0}).format(value) : '—';
export const decimal = value => Number.isFinite(value) ? value.toFixed(1) : '—';
export const option = (value,label) => `<option value="${esc(value)}">${esc(label)}</option>`;
export const avatar = (p,size='sm') => `<span class="ui-avatar ui-avatar-${['xs','sm','md','lg'].includes(size)?size:'sm'}" data-ui-player="${esc(p.player_id||p.id||'')}" data-player-team="${esc(p.team||'')}" aria-hidden="true">${esc(p.position==='DST'?p.team:String(p.player_name||'').split(' ').map(s=>s[0]).slice(0,2).join(''))}</span>`;
export const teamMark = (team,size='lg') => `<span class="ui-team-mark ui-team-${size==='sm'?'sm':'lg'}" data-ui-team="${esc(team)}" aria-hidden="true">${esc(team)}</span>`;
export const positionBadge = position => `<span class="ui-position ui-position-${['QB','RB','WR','TE','DST'].includes(position)?position:'other'}">${esc(position)}</span>`;
const iconPaths={trophy:'M8 3h8v6a4 4 0 0 1-8 0V3ZM8 5H4v3a4 4 0 0 0 4 4m8-7h4v3a4 4 0 0 1-4 4M12 13v6m-4 2h8m-7-2h6',shield:'M12 3 4 6v6c0 5 8 9 8 9s8-4 8-9V6l-8-3Z',upside:'m4 17 6-6 4 3 6-9m-6 0h6v6',link:'M9 15 15 9m-6-2 2-2a5 5 0 0 1 7 7l-2 2m-1 3-2 2a5 5 0 0 1-7-7l2-2',lock:'M7 10V7a5 5 0 0 1 10 0v3M5 10h14v11H5V10Zm7 4v3',unlock:'M7 10V7a5 5 0 0 1 9-3M5 10h14v11H5V10Zm7 4v3',ban:'M5 5 19 19M12 2a10 10 0 1 0 0 20 10 10 0 0 0 0-20',bookmark:'M6 3h12v18l-6-4-6 4V3Z'};
export const icon = name => `<svg class="ui-symbol" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="${iconPaths[name]||iconPaths.link}"/></svg>`;
export const avatarGroup = (players,max=4) => `<div class="ui-avatar-group" aria-label="${esc(players.map(p=>p.player_name).join(', '))}">${players.slice(0,max).map(p=>`<span title="${esc(p.player_name)}">${avatar(p,'md')}</span>`).join('')}${players.length>max?`<span class="ui-avatar-more">+${players.length-max}</span>`:''}</div>`;
export function confidenceMeter(value){const score=Number(value);return value!==null&&value!==undefined&&value!==''&&Number.isFinite(score)?`<span class="ui-confidence" title="Model confidence score, not a win probability"><strong>${esc(value)} / 100</strong><span role="meter" aria-label="Model confidence score" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${Math.max(0,Math.min(100,score))}"><i style="width:${Math.max(0,Math.min(100,score))}%"></i></span></span>`:'—';}
export const sportsbookBadge = book => `<span class="ui-book">${esc(({draftkings:'DraftKings',fanduel:'FanDuel',betmgm:'BetMGM',caesars:'Caesars'})[String(book).toLowerCase()]||book||'Market average')}</span>`;
export function stackPreview(players){if(!players.length)return '';const qb=players.find(p=>p.position==='QB');return `<div class="ui-stack-preview"><span class="ui-stack-label">${icon('link')} Stack connection</span>${players.map(p=>`<div class="ui-stack-player">${avatar(p,'xs')}<span><strong>${esc(p.player_name)}</strong><small>${esc(p.team)} · ${esc(p.position)}${qb&&p.team!==qb.team?' · Bring-back':''}</small></span></div>`).join('')}</div>`;}
export function histogramMarkers(p){const low=p.histogram[0]?.from,high=p.histogram.at(-1)?.to;if(!Number.isFinite(low)||!Number.isFinite(high)||high<=low)return '';return [['Projection',p.final_projection,'projection'],['Median',p.median,'median'],['P25',p.p25,'percentile'],['P75',p.p75,'percentile'],['Sportsbook',p.market_projection,'market']].filter(([,v])=>Number.isFinite(v)&&v>=low&&v<=high).map(([name,value,kind])=>`<span class="ui-chart-marker ui-marker-${kind}" style="left:${100*(value-low)/(high-low)}%" title="${name}: ${decimal(value)} points"><span>${name} ${decimal(value)}</span></span>`).join('');}
export function matchupGraphic(game){return `<div class="ui-matchup-hero"><div class="ui-matchup-team">${teamMark(game.awayTeam)}<strong>${esc(game.awayTeam)}</strong><small>Away</small></div><div class="ui-matchup-center"><span class="ui-tag caution" title="Saved game inputs; see Data details for capture time">Archived matchup</span><span class="ui-matchup-vs">VS</span><span>${esc(new Date(game.market.start_time).toLocaleString(undefined,{weekday:'short',hour:'numeric',minute:'2-digit'}))}</span><div class="ui-field-mark" aria-hidden="true"><i></i><i></i><i></i><i></i><i></i></div><small>Game total <b>${decimal(game.market.total)}</b></small></div><div class="ui-matchup-team">${teamMark(game.homeTeam)}<strong>${esc(game.homeTeam)}</strong><small>Home</small></div></div>`;}
export function relativeTime(value,now=Date.now()){const age=now-Date.parse(value);if(!Number.isFinite(age))return 'Time unavailable';if(age<0)return 'Just updated';const minutes=Math.floor(age/60000);return minutes<1?'Just updated':minutes<60?`${minutes}m ago`:minutes<1440?`${Math.floor(minutes/60)}h ago`:`${Math.floor(minutes/1440)}d ago`;}
